#!/usr/bin/env sh
# This is the script that i3 runs to either start tmux in
# the dropdown terminal or log into a previous session.
tmux kill-session -t music
tmux new-session -s music "tmux source-file ~/.config/ncmpcpp/tmux_session"
